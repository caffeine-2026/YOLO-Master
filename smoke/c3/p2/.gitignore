# C3 experiment artifacts are versioned; binary payloads are stored through Git LFS.
!artifacts/
!artifacts/**

# Checkpoints stay local; compact metrics, manifests, logs, and adapter evidence remain publishable.
artifacts/**/weights/
